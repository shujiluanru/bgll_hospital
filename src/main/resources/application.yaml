spring:
  application:
    name: bgll_hospital
  profiles:
    active: dev
  datasource:
    driver-class-name: ${mysql.driver-class-name}
    url: ${mysql.url}
    username: ${mysql.username}
    password: ${mysql.password}
  data:
    redis:
      host: ${redis.host}
      port: ${redis.port}
      password: ${redis.password}
  ai:
    openai:
      api-key: ${openai.api-key}
      base-url: ${openai.base-url}
      chat:
        options:
          model: ${openai.chat.model}
          temperature: ${openai.chat.temperature}
      embedding:
        options:
          model: ${openai.embedding.model}
          dimensions: ${openai.embedding.dimensions}
server:
  address: 0.0.0.0
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.cug.domain.pojo
bgll:
  sms:
    host: ${sms.host}
    path: ${sms.path}
    templateId: ${sms.templateId}
    appcode: ${sms.appcode}

  jwt:
    user:
      name: ${jwt.user.name}
      secret: ${jwt.user.secret}
      expiration: ${jwt.user.expiration}
  ai:
    rag:
      pdf:
        introduction:
          path: ${rag.pdf.introduction.path}
        category:
          path: ${rag.pdf.category.path}