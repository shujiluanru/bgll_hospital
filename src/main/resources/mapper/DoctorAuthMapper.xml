<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cug.mapper.DoctorAuthMapper">
  <select id="getDoctorByPhone" resultType="com.cug.domain.pojo.Doctor">
    select d.id,
           d.department_id,
           a.name as departmentName,
           d.name,
           d.email,
           d.phone,
           d.password,
           d.description
    from doctor d left join department a
        on d.department_id = a.id where phone = #{phone}
  </select>
</mapper>